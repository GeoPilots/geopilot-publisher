# Screenshots

This directory contains screenshots for the GeoPilots project.

## Guidelines

- Use descriptive filenames that indicate what is shown (e.g., `dashboard-overview.png`)
- Include dates in filenames if tracking UI changes over time
- Recommended format: PNG for UI screenshots
- All screenshot files are tracked with Git LFS

## Organization

You may create subdirectories to organize screenshots by feature or version.
